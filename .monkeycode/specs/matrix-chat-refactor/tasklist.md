# 需求实施计划

- [x] 1. 修复 Agent repr 混合文本解析
  - [x] 1.1 在 `src/lib/a2ui/parser.ts` 中限定 Agent repr 的解析边界，保留 repr 前后的普通文本，并支持同一消息中的多个 repr 区段（附带方案阶段 0、需求 6.3）。
  - [x] 1.2 在 `src/lib/a2ui/agent-repr.test.ts` 中覆盖前缀、后缀、双侧文本和多个 repr 区段的解析结果（附带方案阶段 0、需求 6.3）。

- [x] 2. 显示 AgentTeams workflow 卡片
  - [x] 2.1 扩展 `DisplayMessage` 与 Matrix 格式化逻辑，安全读取 `agentteams.workflow` 结构化字段（附带方案阶段 1）。
  - [x] 2.2 扩展 A2UI 解析块类型，并在 `MessageBubble` 中分发 workflow 块到专用卡片（附带方案阶段 1、需求 6.1）。
  - [x] 2.3 实现 workflow 卡片的运行状态、子智能体和步骤进度展示，兼容字段缺失和未知状态（附带方案阶段 1）。
  - [x] 2.4 为 Matrix workflow 提取、解析块分发和卡片渲染编写单元测试（附带方案阶段 1、需求 6.1）。

- [x] 3. 实现工具调用渲染器注册表
  - [x] 3.1 定义稳定的工具调用载荷，创建按工具名选择渲染器的注册表与通用兜底渲染器（附带方案阶段 3、需求 6.3）。
  - [x] 3.2 实现 read file、write file、apply patch、web search、execute command 和 list directory 的专用渲染器，并安全显示无法解析的参数或结果（附带方案阶段 3）。
  - [x] 3.3 将工具调用块接入 `MessageBubble` 的单遍块渲染流程，保留现有思考、确认、A2UI 与 Markdown 呈现（附带方案阶段 3、需求 6.1-6.3）。
  - [x] 3.4 为注册表匹配、兜底行为和代表性工具卡片编写单元测试（附带方案阶段 3、需求 6.3）。

- [x] 4. 优化虚拟消息列表交互
  - [x] 4.1 使用 `react-virtuoso` 实现消息索引定位与跳转高亮，保留分页锚点和底部跟随机制（附带方案阶段 4、需求 1.1-1.5）。
  - [x] 4.2 为跳转高亮添加短暂、可访问的视觉反馈，并确保动态高度消息测量保持正确（附带方案阶段 4、设计正确性属性 1）。
  - [x] 4.3 为列表定位、跳转高亮和滚动状态编写组件测试（附带方案阶段 4、需求 1.1-1.5）。

- [x] 5. 检查点 - 确保所有默认测试通过
  - 本轮范围不包含服务端 SSE 透传和 AgentTeams Bot 协议升级；它们需要单独的服务端边界与外部仓库变更评审。
